# Supabase Configuration (Required)
VITE_SUPABASE_URL=https://your-project-id.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-public-key-here

# ============================================
# FOR VERCEL DEPLOYMENT (set in Vercel dashboard)
# ============================================

# Anthropic API Key (Required for AI chat feature)
# Set this in Vercel Dashboard > Project Settings > Environment Variables
ANTHROPIC_API_KEY=your-anthropic-api-key

# Supabase credentials for serverless functions
# These are used by the API routes to verify auth tokens
# Set these in Vercel with the same values as the VITE_ versions
SUPABASE_URL=https://your-project-id.supabase.co
SUPABASE_ANON_KEY=your-anon-public-key-here

# ============================================
# FOR LOCAL DEVELOPMENT ONLY (optional)
# ============================================

# If you want to run with the Express backend locally:
# VITE_API_URL=http://localhost:3001

# For local development with Vercel serverless functions:
# Run `vercel dev` instead of `npm run dev`
