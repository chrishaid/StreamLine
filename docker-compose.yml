version: '3.8'

services:
  postgres:
    image: postgres:15-alpine
    container_name: streamline-postgres
    restart: unless-stopped
    environment:
      POSTGRES_USER: streamline
      POSTGRES_PASSWORD: streamline_dev_password
      POSTGRES_DB: streamline_dev
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U streamline"]
      interval: 10s
      timeout: 5s
      retries: 5

volumes:
  postgres_data:
    driver: local
